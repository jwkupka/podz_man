[Unit]
Description=Start tmux in detached session

[Service]
Type=forking
ExecStart=/usr/bin/tmux new-session -s %u-daemon -d
ExecStop=/usr/bin/tmux kill-session -t %u-daemon

[Install]
WantedBy=default.target