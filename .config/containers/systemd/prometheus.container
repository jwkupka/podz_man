[Container]
ContainerName=prometheus
Image=docker.io/prom/prometheus:latest
AutoUpdate=registry
Volume=prometheus-data:/prometheus
Volume=%h/etc/prometheus.yml:/etc/prometheus.yml
Network=podz.network

[Service]
Restart=always

[Install]
WantedBy=default.target
